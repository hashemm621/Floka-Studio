import React from 'react';
import Navbar from './shared/Navbar';

const Root = () => {
    return (
        <div>
           <nav>
            <Navbar/>
           </nav>
        </div>
    );
};

export default Root;